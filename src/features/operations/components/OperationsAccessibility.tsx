import { AccessibilityMenu } from "@/components/a11y";

/** Operations Console uses the shared MediKiosk accessibility panel. */
export function OperationsAccessibility() {
  return <AccessibilityMenu />;
}

import { AccessibilityMenu } from "@/components/a11y";

/** Assisted Kiosk uses the shared MediKiosk accessibility panel. */
export function AssistedAccessibility() {
  return <AccessibilityMenu />;
}

"use client";

import { ClinicianPlaceholder } from "@/features/clinician/components/ClinicianPlaceholder";

export default function ClinicianSettingsRoute() {
  return (
    <ClinicianPlaceholder
      title="Clinician settings"
      description="Preferences for language defaults, notifications and display density."
    />
  );
}
